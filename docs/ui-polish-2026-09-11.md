# 偏好与自由合成视觉修整工单

状态：实现完成，浏览器验证通过；真实窗口验收 partial / open。

## 交付与依赖

- [x] A：一起做事、角色卡设置统一为横向设置行，限制控件宽度，统一标题、操作与只读信息。可独立施工。
- [x] B：角色与对话加入当前角色头像预览、更换、裁剪、上传与错误反馈；复用原角色头像 API，切换角色后不能串写。依赖 A 的设置布局。
- [x] C：自由合成改为视觉小说式舞台：主聊天区、侧边信息卡、浅层叠边框、虚实连接线、悬停反馈；去掉默认窗外 Halo/浮岛，兼容窄窗口与减少动态效果。可与 A/B 独立施工。
- [x] D：类型检查、构建、相关回归与浏览器验证，更新架构和已知问题。
- [ ] E：真实 Tauri 多 DPI、窗口切换、真实后端头像与手机同步验收（open，不以静态检查代替）。

## 三面闭环

设置布局与 Design Mod 属桌面本地呈现，无新增后端配置、持久化或 WS 契约。头像沿用
get/upload_character_avatar → 后端角色头像 → prompt-assets 失效 → activeCharacter revision
通知链；模型配置仍由后端管理面维护。回查后端总账和手机消费者，只读跨仓检查；本单
不修改其他仓库，需同步的总账说明记录在本仓，保持 open。

## 验收记录

TypeScript 与生产构建通过（已有大 chunk 提示）；相关 Vitest 105 项通过。
`node scripts/ui-polish-browser.mjs` 在 Edge headless、模拟 Tauri IPC 下通过：

- 1280/520px 设置行几何对齐，角色切换、单一刷新按钮。
- 头像裁剪上传失败后重试、上传 charId/PNG 校验、切换角色取消未提交裁剪。
- 新舞台真实 React 聊天发送/显示、悬停连接线、1280/900/600px 布局、减少动态效果。
- 20 次 builtin ↔ freeform 切换，无重复输入框，当前包不请求创建 native surface。
- 已目检 `.tmp/ui-character.png`、`ui-activity-narrow.png`、`ui-stage-wide.png`、
  `ui-stage-600.png`。模拟花园错误态仍显示重试按钮，不再显示无说明空框。

真实后端、Tauri 原生多 DPI/多屏、夜间主题、旧 Halo 会话迁移及手机同步为 open；
后端总账待由对应仓库同步，详见 `known-issues.md`。

A：已统一控件宽度、标题操作行、只读值对齐和换行。TypeScript 通过；浏览器几何验收纳入 D。
