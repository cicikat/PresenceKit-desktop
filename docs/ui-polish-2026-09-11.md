# 偏好与自由合成视觉修整工单

状态：施工中。每项实现和验证完成后独立勾选、提交；真实窗口验收单独记录。

## 交付与依赖

- [ ] A：一起做事、角色卡设置统一为横向设置行，限制控件宽度，统一标题、操作与只读信息。可独立施工。
- [x] B：角色与对话加入当前角色头像预览、更换、裁剪、上传与错误反馈；复用原角色头像 API，切换角色后不能串写。依赖 A 的设置布局。
- [ ] C：自由合成改为视觉小说式舞台：主聊天区、侧边信息卡、浅层叠边框、虚实连接线、悬停反馈；去掉默认窗外 Halo/浮岛，兼容窄窗口与减少动态效果。可与 A/B 独立施工。
- [ ] D：类型检查、构建、相关回归与浏览器验证，更新架构和已知问题。
- [ ] E：真实 Tauri 多 DPI、窗口切换、真实后端头像与手机同步验收（open，不以静态检查代替）。

## 三面闭环

设置布局与 Design Mod 属桌面本地呈现，无新增后端配置、持久化或 WS 契约。头像沿用
get/upload_character_avatar → 后端角色头像 → prompt-assets 失效 → activeCharacter revision
通知链；模型配置仍由后端管理面维护。回查后端总账和手机消费者，只读跨仓检查；本单
不修改其他仓库，需同步的总账说明记录在本仓，保持 open。

## 验收记录

待施工后逐项补充。

A：已统一控件宽度、标题操作行、只读值对齐和换行。TypeScript 通过；浏览器几何验收纳入 D。
