# 24 — 死代码与迁移化石清理(P1,可并行,建议最先做)

## 审计确认

- `sensor-service/` 整个目录已废弃(感知已嵌入 `src-tauri/src/sensor/`),无运行时引用,
  仅剩 `.gitignore` 两行(L1 注释、L19 config.yaml)和文档叙述。
- `src/windows/chat/components/archive/Sidebar.legacy.tsx`、`SpecPanel.legacy.tsx`:
  全仓无外部引用,纯存档。

## 交付物

1. `git rm -r sensor-service/`;`git rm -r src/windows/chat/components/archive/`。
   历史在 git 里,不需要 archive 目录。
2. 清 `.gitignore` 中 sensor-service 相关行。
3. 清文档叙述:
   - `AGENTS.md`:删"该目录为已废弃骨架,后续清理"句,改为"感知嵌入 `src-tauri/src/sensor/`";
     密钥段落删 `sensor-service/config.yaml`。
   - `CLAUDE.md` 路径约定段:同上删 `sensor-service/config.yaml`。
   - `ARCHITECTURE.md`:目录表删 `sensor-service/` 行;鉴权三类 token 段把"已废弃的手机
     sensor-service(本仓)"改为历史注记(token 不复用的告诫保留,因为后端 token 还在)。
   - `docs/frontend-structure.md` 等如提及 archive/ 一并清理。
4. 全仓 grep `sensor-service`、`Sidebar.legacy`、`SpecPanel.legacy` 确认零残留
   (排除 node_modules/dist/src-tauri/target)。

## 验收

- `npm run build`(或 `npx tsc --noEmit` + vite build)通过。
- grep 零残留。

## 依赖

无。**建议在 25(truthfulness pass)之前做**,避免文档改两遍。
