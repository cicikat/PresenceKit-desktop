# 聊天发送、重试与渲染修复

范围：本客户端；手机与后端只读核对，既有未提交产物卡改动保留。

- [x] 1. 点击发送立即将附件/引用从输入框移到用户消息，保留失败重试快照。
- [x] 2. 参考手机在失败的原消息上提供重试，复用消息 ID，不覆盖新草稿。
- [x] 3. 流式样式标签及时渲染，保持安全白名单与 canonical 对账。
- [x] 4. 公告版本改为 v1.1.0，文案使用语义 i18n key。
- [x] 5. 回归测试、TypeScript、生产构建及可行的浏览器验证。

依赖：2 依赖 1 的发送快照；3、4 可独立实施。本次顺序施工，每项验证后勾选。

三面检查：发送/上传继续使用原 Tauri HTTP 桥、Bearer/scope 与 WS msg_id 去重；不新增后端设置、落盘、队列、ack 或 TTL。手机 retryMessage 作为失败消息重发的交互参考。真实 Tauri、手机和后端联合验收保持 open；后端总账同步因本仓范围边界留交接。

验证：`npm.cmd test` 62 文件/256 项通过；`npx.cmd tsc --noEmit`、`npm.cmd run build` 通过。
`node scripts/chat-send-retry-browser.mjs` 隔离真实后端，实际挂载 React：延迟请求期间清空、
文字/图片失败重发 payload 一致、无重复用户气泡、新草稿保留、流式样式即时生效及 canonical 去重通过。
截图已目检。生产构建仍有既有大 chunk 提示。

open：真实 Tauri、文件路径上传/手机/真实模型联合与 release 验收；后端总账同步。
原接口不提供客户端幂等键，结果不明后的手动重试不保证后端恰好执行一次；本单不擅自改契约。

提交说明：施工期间并行提交 `711ec4f` 将共享 ChatPanel、语言包、前端与已知问题文档中的
本单修改一并提交。本单保留该历史，仅补交新模块、回归脚本、公告页面与施工记录。
