# 26 — v0.1 发布范围冻结(P1,弱依赖 21)

## 背景

`package.json`/`tauri.conf.json` 版本已是 0.1.0,但无 release scope 文档;known-issues 里
TTS、v1 协议、state_update、花园交互等以"未迁完/未接入"状态悬着,拖住发布判断。

## 交付物

新建 `docs/release-v0.1.md`:

**IN(已具备,发布即含)**:主聊天窗口全功能(含流式、分段、群聊)、pet 窗口(粒子/3D/Live2D
+ 鼠标互动)、花园只读展示、日记/聊天日志只读、Dream/Toy/Activity 现有形态、潜意识只读面板、
sensor 嵌入式采集、连接设置页、9 类 desktop action(见 protocol-v0.md)。

**OUT(明确不含,post-v0.1)**:

- TTS 语音播放(known-issues 首条)
- v1 WS 协议全套(assistant_message/state_update/user_message/client_event/envelope/capabilities)
- 用户输入 WS 化(HTTP `/desktop/chat` 即正式路径)
- 花园交互化(浇水/harvest/vase 操作)
- 日记 emotion 展示增强
- flow/status 面板完整化
- 完整具象 Pet 行为体系(现有行为集即 v0.1 形态)

每条 OUT 项注一句"现状可用形态是什么",避免误解为不可用。

同步动作:

1. `docs/known-issues.md` 对应条目标 `post-v0.1`。
2. `AGENTS.md` 当前实际状态段落末尾加一行指向本文档。

## 需要用户确认的点(仅一处)

OUT 清单是我按 known-issues + 文档现状拟的默认判断;若茶茶想把某项(如花园浇水)拉回 v0.1,
在执行前说一声即可,其余照默认。

## 依赖

弱依赖 21(引用 protocol-v0.md 的 action 清单;可先写、后补链接)。
