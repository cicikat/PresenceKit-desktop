# 28 — 三仓 known-issues 清盘(合并工单)

> 范围:Emerald-presence / Emerald-client / Emerald-mobile 三仓的 `docs/known-issues.md` 全量清账。
> 目标:债本清零到"每条要么关、要么修、要么显式封存",不再有"记了就算过了"的悬浮条目。
> 本工单含上轮遗留的两处小修(§B-1/B-2)。
> 各节标注执行仓;A/B/C 三节互相无依赖可并行,D 节纯记录。

---

## A. 直接销账(已修但条目没关,只动文档,零代码风险)

以下均已当场核对过代码,条目落后于现实:

### A-1 [presence] ADMIN-1:jailbreak_entries.py 缺 Path 导入 → **已修**
`admin/routers/jailbreak_entries.py:8` 已有 `from pathlib import Path`。条目移到已关闭区。

### A-2 [presence] F11 剩余工作:memory 工具接入对话循环 → **已被 Brief 28/29 完成**
`core/pipeline.py:836` 生成侧 tool loop 默认 categories 已含 `"memory"`。F11 整条改 `fixed`,注明由 tool loop 落地关闭。

### A-3 [presence] P2:`_layer` 透传给 LLM → **已修**
`core/llm_client.py` 已引入 `core/prompt_layer.sanitize_messages`(API-safe 剥离 `_layer`)。核对 `chat()` 调用点确实走 sanitize 后关闭条目。

### A-4 [presence] PB3:episodic 静默归零风险 → **已修**
`episodic_memory.py:76` 加载失败已 `raise EpisodicCorruptError`(fail-loud);`safe_write.py:58-65` 已有写后校验 + `.bak`。确认"拒绝空列表覆写"护栏是否也在,在则整条关闭,不在则只留该一小点。

### A-5 [presence] TEST-1:测试断言旧 datapath → **大部分已过时**
`test_sandbox_paths.py:23` 断言的已是 `runtime/channel_queue.json`(新布局);`_identity_file` 全仓零命中。逐条核对后关闭或收缩条目。

### A-6 [presence] known-issues 文件本身的卫生
"当前仍存在"一节里躺着 9 条 `fixed` 状态条目(B11、F10、D2、P1、SEC-AUTH-1、SEC-WS-1、identity-1、TD-2、TD-3)。全部移到"本轮已核对关闭"表或直接删(git 有历史)。文件从 ~730 行瘦身,让"仍存在"名副其实。

### A-7 [client] Panes.tsx 两条目自相矛盾
上方 P2 说"存在历史 TS 报错",底部已修复区说 2026-05-16 已清零。跑一次 `npx tsc --noEmit`(本机跑,沙箱太慢)定案:干净则删 P2 条目,不干净则修掉报错后两条都关。

### A-8 [client] 花园只读 + 日记 emotion 两条 → 改挂 post-v0.1
release-v0.1.md 已把花园交互、日记 emotion 列为 OUT,known-issues 里对应两条(P2 花园只读、P2 日记 emotion)加 `post-v0.1` 标签 + 指向 release-v0.1.md,与 TTS 条目同格式。

---

## B. 顺手小修(半天内全部可完成,彼此独立可并行)

### B-1 [client] backend-integration.md L213 口径残留(上轮遗留)
"旧 v1 方案写明…当前尚未做到"与 protocol-v0"正式路径,不是过渡态"冲突。改写为:"旧 v1 方案的 WS user_message 设想已降级为 post-v0.1 roadmap,见 protocol-v0.md;当前 HTTP 路径为正式契约。"

### B-2 [client] ChatPanel 三处内联 `15000`(上轮遗留)
`ChatPanel.tsx:727/1471/1544` 的 `now - r.renderedAt < 15000` 改用 `correlation.ts` 的 `RENDERED_FALLBACK_TTL_MS` / `pruneRenderedFallbacks()`,消除常量漂移。

### B-3 [client] P3:tauri-app 模板名统一
`package.json` name、`tauri.conf.json` productName / window title、`Cargo.toml` description 改为 PresenceKit-desktop。**注意**:绝不动 `identifier`(改它会清零 WebView2 profile,见 known-issues 2026-07-05 教训——虽然 uiPreferences 已有文件后端,仍不要动)。

### B-4 [client] P3:header"偏好"按钮无 onClick
传 `onOpenPrefs` 或删除该重复入口。二选一,建议接上(入口冗余无害,死按钮有害)。

### B-5 [client] P3:system 消息按 HER 气泡渲染
`Bubble` 给 `role === 'system'` 单独浅灰居中样式,连接失败提示不再像角色发言。

### B-6 [client] SubFlow 时间轴按激活角色分桶(解锁 presence ACT-1)
`SubFlow.tsx:63` `STORAGE_KEY = 'subflow_timeline'` 是全局单桶——这正是后端 ACT-1 审计指认的"阅读动向串角色"嫌疑点(前端侧)。改为 `subflow_timeline:{charId}`(用 `getActiveCharacterInfo().id`),迁移:旧 key 数据一次性归入当前激活角色后删除。完成后到 presence 仓把 ACT-1 状态更新为"前端已分桶,待复现观察"。

### B-7 [presence] PB2:桌宠 avatar 身份锚点
prompt 层加一句硬锚点:"屏幕上的桌宠形象是你自己在屏幕上的存在,不是用户的角色";空闲回归触发套用 doc-1 反编造规则(无真实屏幕感知不虚构屏幕场景)。改 `prompt_builder.py` 相应层 + 跑 `python tests/run_eval.py`。PB2 条目随之关闭或降为 observe。

### B-8 [mobile] P2:悬浮窗硬编码示例订单
`FloatingBubbleService.kt` `addOrderContent()` 删掉"香菇滑鸡饭…39.00"假数据,未接真实数据前只显示中性确认文案。这条挨着"不自动加购/支付"安全边界,假订单最容易造成误解,mobile 仓优先级最高的一条。

### B-9 [mobile] P3:通知权限请求时机
`MainActivity.onCreate()` 的 `requestNotificationPermission()` 移到能力检查页/首次开启后台通知时。

---

## C. 独立中型任务(不属于顺手,单独排期,按优先级排序)

### C-1 [client] P1:Dream 打开时 ChatPanel 卸载,park/flush 失效 — **建议最先做**
条目里唯一影响运行时正确性的真 bug:park 逻辑已写好但挂载结构让它形同虚设。照 Activity overlay 的做法保持 ChatPanel 挂载、Dream 期间只隐藏 UI,然后端到端验证退梦 flush。

### C-2 [presence] ACT-2:流式路径反坍缩
需要设计(暂缓推送前 N token vs 接受软降级),不是小修。维持 `now-safe-to-fix(需独立设计)`,建议排进下一个后端窗口。

### C-3 [presence] H1:hidden_state 现实写入接线
工单已有(`cc-tasks/08b`),面板观测点已备好(`SubHiddenStatePanel` 的 time_decay 提示),卡的只是"哪些现实信号映射哪种 RealityEventType"这一个拍板。**建议拍板并排期**——这是所有 boundary-doc-needed 里唯一"设计 90% 完成、只差临门一脚"的,再放就真成永久债了。

### C-4 [client] ChatPanel 对账 timer 竞态测试(上轮记债)
correlation.ts 目前只测了无状态工具函数,timer 竞态时序(WS 先到取消/timer 先触发跳过/兜底表命中)零覆盖。作为独立条目记入 client known-issues,post-v0.1 排期。

---

## D. 显式封存与用户动作(本工单只改状态标注,不做事)

### D-1 设计决策债 → 收进一个 `design-backlog` 段落
D7(日记反向进长期认知)、G4(花园采后容器)、DESIGN-1/2(感知/主动行为边界)、SC1(酒馆卡导入,维持冻结)、REC1(召回启发式)、P2-1(read_diary 复读旁路)、PB1(等召回修复后复评)——共同点是**等设计拍板,不是等人写代码**。在 presence known-issues 里合并为一个"设计后备箱"章节,每条一行 + 指向原详情(git 或原文附录),不再逐条占"当前仍存在"版面。想启动哪条时单独开工单。

### D-2 纯用户动作(代码侧无事可做,列清单给茶茶)
- [presence] SEC-AUTH-2 P4 后半:各持有方实际切换新 token → ESP32 重烧录 → Watch Shortcut / 管理面板换值 → 最后轮换 legacy secret。
- [presence] audit.jsonl 里 ~200 条 testclient 测试噪音,决定是否手动清空。
- [mobile] ntfy 中继配置(P0 弹窗的真正卡点):起 ntfy → config.yaml 填 relay_* → App 填同组地址 → 真机验收。
- [mobile] release keystore(仅在打算对外发布时)。
- [mobile] flutter tester 回环故障:本机环境问题(防火墙/安全软件),修好后跑全量 `flutter test`。

### D-3 观察项维持原状(有到期日的加提醒)
- [presence] PB4:Path B 观察期 **2026-08-10 到期**——到期无缺口记录就开整删 brief。在条目顶加显眼倒计时行。
- [presence] identity-2 冷启动、Brief 28/29 观察项:维持 observe。
- [client] P2 garden daily 端到端体感:维持观察(依赖真实使用周期)。
- [mobile] P3 boot receiver:自用阶段维持接受;能力页已可察觉失活。

---

## 验收

1. 三仓 known-issues 的"当前仍存在/未修"区里,每条都有明确状态:open(带下一步)/observe(带条件)/post-v0.1/design-backlog,零"fixed 却躺在仍存在区"。
2. B 节代码项:client 跑 `npx tsc --noEmit` + vitest;presence 改 prompt 后跑 `tests/run_eval.py`;mobile 跑 `flutter analyze`(tester 坏着,静态分析兜底)。
3. B-6 完成后同步更新 presence ACT-1 条目。
4. 每个独立修复单独 commit(三仓协作偏好均已预授权)。

## 并行度

A 节全部并行(纯文档);B 节 9 条全部并行;C-1 独立可并行;C-3 需茶茶拍板映射表后开工。
唯一顺序约束:A-6(presence 文件瘦身)最后做,等 A-1~A-5 和 B-7 的状态变更先落进去。
